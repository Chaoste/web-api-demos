import { css } from "@emotion/css";

export const styles = {
  root: css({
    display: "flex",
    flexDirection: "row",
    gap: 16,
    alignItems: "center",
    justifyContent: "center",
  }),
};
